<!DOCTYPE html>
<html>
<head>
<title><%= title %></title>
</head>
<body>
<h1><%= title %></h1>
<ul>
<% students.forEach((student) => { %>
<li>
<%= student.id %> -
<%= student.name %> -
<%= student.major %>
</li>
<% }); %>
</ul>
<p>
<a href="/">Back to Home</a>
</p>
</body>
</html>